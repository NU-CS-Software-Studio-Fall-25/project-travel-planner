<div class="container my-4">
  <div class="hero-section text-center py-5">
    <h1>Travel Planner</h1>
    <p class="lead">Discover your perfect destination with personalized, safe, and practical travel recommendations!</p>

    <% if logged_in? %>
      <div class="alert alert-success mb-4 rounded shadow">
        <h4>Welcome back, <%= current_user.name %>! 👋</h4>
        <p>Let's plan your next adventure—explore new places now.</p>
      </div>
      <div class="cta-buttons d-flex justify-content-center gap-3 mb-4">
        <%= link_to "Get AI Recommendations", recommendations_path, class: "btn btn-primary btn-lg" %>
        <%= link_to "My Profile", current_user, class: "btn btn-outline-secondary btn-lg" %>
        <%= link_to "Browse Destinations", destinations_path, class: "btn btn-outline-info btn-lg" %>
      </div>
    <% else %>
      <div class="jumbotron p-4 mb-4">
        <h2 class="display-6">Create Your Account and Get Personalized Suggestions!</h2>
        <p class="lead">Enter your preferences, budget, and passport country for AI-powered destination tips.</p>
        <div class="mt-4 d-flex flex-wrap justify-content-center gap-3">
          <%= link_to "Create Account", signup_path, class: "btn btn-success btn-lg" %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<% unless logged_in? %>
  <div class="features-section mt-5 container">
    <h2 class="text-center mb-4">Why Use Travel Planner?</h2>
    <div class="row">
      <div class="col-md-4 mb-3">
        <div class="card h-100 text-center shadow feature-card feature-card-visa">
          <div class="card-body">
            <h3>Visa & Safety Check</h3>
            <p>Your passport, visa needs, and latest safety info for every destination.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4 mb-3">
        <div class="card h-100 text-center shadow feature-card feature-card-ai">
          <div class="card-body">
            <h3>AI Recommendations</h3>
            <p>OpenAI finds destinations matching your preferences, budget, and travel goals.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4 mb-3">
        <div class="card h-100 text-center shadow feature-card feature-card-planning">
          <div class="card-body">
            <h3>Integrated Planning</h3>
            <p>Google Maps and TripAdvisor: get flight, hotel, and activity info instantly.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
