# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# IMPORTANT: SQLite database files should NOT be committed
# Team members will create their own local databases using migrations
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# Database schema and migrations SHOULD be committed (don't add these to gitignore!)
# ✅ db/schema.rb - COMMIT THIS
# ✅ db/migrate/*.rb - COMMIT THESE
# ❌ *.sqlite3 - DON'T COMMIT THESE

/public/assets

# Ignore master key for decrypting credentials and more.
/config/master.key

# Ignore database configuration backups
/config/database.yml.backup
/config/database.yml.pg

# Local environment file for secrets (do NOT commit)
/config/local_env.yml
